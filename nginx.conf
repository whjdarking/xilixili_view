upstream back-service {
    server back-service;
}
server {
    listen 80;
     location / {
       root /usr/share/nginx/html;
       index index.html;
       proxy_pass http://back-service
     }
}
#这份nginx.conf是专门在我的kubernetes上跑而设置的